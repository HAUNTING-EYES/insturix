import { redirect } from "next/navigation";

/** Account seam — resolves to the real platform connections surface. */
export default function AccountConnectionsPage() {
  redirect("/dashboard/uploaderx");
}
