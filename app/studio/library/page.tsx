import { LibraryPlace } from "@/components/studio/shell/places";

export const metadata = { title: "Insturix — Library", robots: { index: false, follow: false } };

export default function LibraryPage() {
  return <LibraryPlace />;
}
