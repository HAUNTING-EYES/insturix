import { BrandsPlace } from "@/components/studio/shell/places";

export const metadata = { title: "Insturix — Brands", robots: { index: false, follow: false } };

export default function BrandsPage() {
  return <BrandsPlace />;
}
