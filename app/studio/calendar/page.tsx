import { CalendarPlace } from "@/components/studio/shell/places";

export const metadata = { title: "Insturix — Calendar", robots: { index: false, follow: false } };

export default function CalendarPage() {
  return <CalendarPlace />;
}
