# Editron OE V2-1 connected mechanics preflight

Date: 2026-08-15

Authority: `RESEARCH_ONLY_NO_PROVIDER_CALL_NO_PROJECT_MUTATION`

## Outcome

The four-task V2-1 development run is not yet dispatchable. DEV-02 has real
Stage 1-6 and repeated generated-island evidence, including Qwen's canonical
1/3 result, but that evidence does not constitute the full hybrid reel chain.
DEV-01, DEV-03 and DEV-04 have frozen Stage-1 packets and generic schemas, not
task-specific Stage 2-4 evaluators/compilers or the required native proxy path.

The generated preflight preserves four challenger identities:

- `gpt-5.6-luna` through the direct OpenAI provider lane;
- `gpt-5.6-terra` through the direct OpenAI provider lane;
- `gemini-3.7-flash` through the direct Google provider lane; and
- `qwen3.8-max` through the separate permitted OpenCode agent-shell lane.

Qwen Token Plan credits are not presented as comparable USD telemetry.

## Budget correction

The V2-0 fixture still says a whole trial may cost at most `$0.67`. Later fair
Stage 1-3 ceilings are `$0.35`, `$0.30` and `$0.20`, or `$0.85` per direct-model
task before deterministic compilation. The old whole-trial figure is therefore
superseded for the connected run rather than silently reused.

For four tasks and three direct routes, the maximum planning ceiling is
`$10.20`. The separately frozen generated-source cohort can reserve at most
`$4.50`, producing a combined direct-provider ceiling of `$14.70`. These are
hard maximum reservations, not predicted spend; previous observed costs were
far lower.

## Exact remaining execution order

1. Complete DEV-04's capability-gap chain and prove a truthful zero-execution
   stop.
2. Complete DEV-01's native multi-operation compiler and bounded proxy.
3. Complete DEV-03's audio/video evidence, native compiler and bounded proxy.
4. Complete DEV-02's full hybrid chain around the already-proven generated
   island and persist a formal real-human Stage-7 receipt.
5. Only then freeze a dispatchable plan hash, obtain exact operator
   confirmation, and run the four development tasks.

No old provider response, Qwen result, human observation or receipt is erased.
It may be reused only if every upstream packet and artifact binding matches the
new connected chain exactly.
