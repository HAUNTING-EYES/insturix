# Editron Codex hardcodes, assumptions and evidence-debt register

Opened: 2026-08-20; last reconciled: 2026-08-30
Authority: code-grounded programme audit; status register, not runtime authority
Audit lane: editron-worktree / infrastructure-improvs-+Editron
Audit HEAD: findings rechecked through `f7abef209`;
latest route-ablation paid-cohort receipt `9583de5c0eb3281ea780e82cca4d0b735c1ec36f41970d6d7d3bfbe5031a498f`

## Purpose

This document records the material hardcodes, authored benchmark choices,
architectural assumptions, invalid earlier evidence and unresolved production
claims introduced or relied upon during the Editron work from 2026-08-10
through 2026-08-20.

It deliberately distinguishes four things that were previously blurred:

1. a frozen synthetic or diagnostic fixture;
2. a bounded research choice;
3. a provisional production hypothesis that still needs calibration;
4. a hidden production assumption or defect that must be removed.

This is not a list of every numeric literal in the repository. It is the
material decision register found by auditing the master plan, current code,
Git history, benchmark artifacts, the existing benchmark postmortem and the
active task transcript. A newly discovered material assumption must be added
here; it must not be silently explained away.

## Status vocabulary

| Flag | Meaning |
| --- | --- |
| FIXTURE_FROZEN | Intentional value belonging only to one versioned test fixture. It may not be generalized to product behaviour. |
| RESEARCH_BOUND | Deliberate experiment limit used for fairness, cost or safety. It is not a production limit. |
| PROVISIONAL_POLICY | Plausible design choice that has not passed the required calibration or held-out evaluation. |
| KNOWN_DEFECT | Current code can produce an incorrect or unsafe result because of this assumption. |
| MISSING_CONTRACT | The production identity, owner, policy or proof does not yet exist. |
| HISTORICAL_INVALID_EVIDENCE | A prior score or conclusion did not test what it claimed. Preserve it for diagnosis; never use it for model promotion. |
| USER_DECISION | An explicit operator decision rather than a measured technical conclusion. |
| VERIFIED_CURRENT | Reproduced directly from the active branch at the audit HEAD. |

## Audit boundary and evidence

- The active worktree was dirty before this document. No reset, clean, stash,
  merge, commit or push was performed.
- The local annotated tag editron-interface-freeze-1 still resolves to
  5a47e00896e0e915cd4c03e71a0b104ac0c05999. Neither that commit nor Phase 2C
  7e9b4dd7ff60beeef2b6dfff4038ca367164cb65 is an ancestor of the active HEAD.
- The active task transcript examined for assumption discovery is
  C:/Users/admin/.codex/sessions/2026/08/09/rollout-2026-08-09T13-46-08-019fe597-f478-71a1-9e15-e44ff8230c0a.jsonl.
  It is supporting discovery evidence, not current code truth.
- The prior, detailed benchmark-error evidence remains authoritative in
  [the benchmark reconciliation postmortem](./oe-agentic-editorial-planning-and-benchmark-reconciliation-2026-08-17.md#complete-material-error-ledger).
- Current implementation status remains governed by
  [the master execution ledger](../../EDITRON_FINAL_EXECUTION_PLAN_2026-08-10.md#authoritative-code-grounded-execution-ledger---2026-08-17).

## A. HREF-01 reference-observation assumptions

| ID | Hardcode or assumption | Flag | Current evidence and required disposition |
| --- | --- | --- | --- |
| HREF-01 | Fourteen samples at 0.5, 5, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55, 60 and 64.25 seconds. | FIXTURE_FROZEN | The 64.75-second, 60-fps source contains 3,885 frames. Fourteen images cover about 0.36 percent of source frames. This is a sparse ordered-image transport and broad-observation arm, not a production reference-understanding sampling policy. Source: provider-native-reference-holdout-01-v2r.ts lines 20–35. |
| HREF-02 | Frames are downscaled to 960x540 JPEG, MJPEG quality 2, yuvj420p, one thread, with one exact 2018 FFmpeg binary and byte hashes. | FIXTURE_FROZEN | This provides byte reproducibility for this machine and fixture. It is not a claim that 540p JPEG is enough for typography, masks, tracking, grading or professional reference reconstruction, and it creates portability debt if the frozen binary disappears. Source: provider-native-reference-holdout-01-v2r.ts lines 70–80 and provider-native-reference-holdout-01-preflight-v2r.ts lines 240–251. |
| HREF-03 | Ordered images deliberately exclude native video and audio. | RESEARCH_BOUND | The arm can test image order, timestamp binding, broad progression and provider serialization. It cannot establish music, dialogue, sound design, exact easing, continuous motion, transition microtiming or unsampled events. These must remain UNVERIFIABLE or request dense/native evidence. |
| HREF-04 | Generic input guards allow at most 64 frames, 8 MiB per frame and 64 MiB total. | RESEARCH_BOUND | These are defensive transport guards in provider-native-reference-input-v2r.ts lines 10–12. They have not been calibrated as production reference-quality limits. |
| HREF-05 | The observer receives one control-only finish function and 4,096 maximum output tokens. | RESEARCH_BOUND | This deliberately prevents editing mutation and keeps the first diagnostic bounded. It does not establish that a one-turn observer is sufficient for complex references or dense reinspection. Source: provider-native-reference-holdout-01-preflight-v2r.ts lines 204–217. |
| HREF-06 | A recurring pattern requires at least two occurrences and relevant counterexamples. | PROVISIONAL_POLICY | This is a sensible anti-hallucination floor, not a universal definition of editorial grammar. It needs multi-reference expert calibration. Source: provider-native-reference-holdout-01-v2r.ts lines 120–126. |
| HREF-07 | The evaluator contains twelve provisional observations authored from one Insturix source. | PROVISIONAL_POLICY | They must be reviewed before any model response is seen. A single author and reference cannot establish general editorial competence. Source: provider-native-reference-holdout-01-evaluator-v2r.ts lines 23–60. |
| HREF-08 | Repository provenance was treated as enough for local preflight, but legal provider-egress clearance is not asserted. | MISSING_CONTRACT | The manifest explicitly says legal clearance is not asserted and provider egress needs separate authorization. No provider dispatch may infer rights from Git provenance. |
| HREF-09 | Luna, Terra and Gemini 3.7 Flash at medium reasoning are the current HREF routes. | RESEARCH_BOUND | This is an experiment roster, not model promotion or permanent routing. Qwen retirement is a USER_DECISION, not proof of technical inferiority. |
| HREF-10 | The planned competence pilot says at least twenty moving references and two independent expert annotations. | PROVISIONAL_POLICY | This is an initial diagnostic minimum, not a statistically certified universal sample size. The project must publish content coverage, annotator competence, agreement, adjudication and confidence before using it for promotion. |
| HREF-11 | Earlier discussion proposed approximately 2/4/8-fps escalation, every-frame inspection when needed and an aggregate 15-minute project reference limit. | PROVISIONAL_POLICY | None is a calibrated production threshold. Production sampling must be claim-conditioned, preserve native video/audio arms, detect missed events and be evaluated against dense ground truth. The 15-minute figure must not become a hidden product cap. |
| HREF-12 | A repair curve of 0/1/2/4/8 attempts was proposed after rejecting a 50-iteration research loop. | PROVISIONAL_POLICY | This is an experiment design. Interactive and asynchronous budgets must be selected from measured quality, latency and cost curves, not hard-coded as doctrine. |
| HREF-13 | A proposed cross-element relationship router included a threshold of four linked elements. | PROVISIONAL_POLICY | The dependency signal is useful, but N greater than or equal to four is not production evidence. It remains a held-out routing hypothesis and must not decide native/generated/hybrid execution without comparison trials. |
| HREF-14 | No production reference-sampling owner or certified policy currently exists. | MISSING_CONTRACT | HREF-01 proves a sparse diagnostic transport only. Production needs source-rate-aware event proposals, audio/native-video support, adaptive dense windows, missed-event measurement, versioned evidence receipts and evaluator coverage. |

## B. Historical benchmark errors that must remain flagged

Every row below is HISTORICAL_INVALID_EVIDENCE unless a narrower disposition is
stated. Later code fixes do not retroactively make the old run valid.

| ID | What was wrong | Permanent interpretation |
| --- | --- | --- |
| BENCH-01 | Early 0/45, 0/45 and 1/45 executable totals measured one-shot verifier-clean serialization. | They are not executed or rendered editing scores. |
| BENCH-02 | Withheld-evidence rows still required hidden evidence-bound mutations. | Impossible condition; invalid evidence. |
| BENCH-03 | Early DEV-02 demanded a native low-level graph and omitted the generated-composition route. | Invalid test of the agreed filmstrip-as-generated-island/full-reel-as-hybrid design. |
| BENCH-04 | Resolver and mutation ports were open or incompatible. | Contract defect, not automatically model failure. |
| BENCH-05 | candidateCapabilityIds mixed executable selections with alternatives. | Replaced by one selectedOperatorId per node and separate alternatives. Old scores remain invalid. |
| BENCH-06 | Models were told compiler-owned adapters could be omitted, but Terra was failed for omitting one. | Prompt/evaluator contradiction; that Terra row is invalid. |
| BENCH-07 | DEV-specific lowerers encoded expected task topology. | Mechanics evidence only; not generic planning proof. |
| BENCH-08 | A compiler patch inserted seven catalog read/search/proof nodes after the model responded. | Architectural drift. The binder must add and drop zero catalog operations. |
| BENCH-09 | Canonical or editor-approved handoffs were substituted for preceding model artifacts. | Valid stage/mechanics evidence only, not connected model orchestration. |
| BENCH-10 | Issued packets were rebuilt from mutable current preflight state. | Historical provenance was contaminated until issued snapshots were pinned. |
| BENCH-11 | DEV-01 product evidence said frame 205 while the proxy exposed it at frame 180. | Fixture/evidence mismatch. |
| BENCH-12 | DEV-01 initially mixed music and a speech-like tone in one source. | Could prove a gain envelope, not dialogue intelligibility. |
| BENCH-13 | The cut receipt lacked original-to-child ID and timeline-coordinate mapping. | Downstream push-in binding could not be proven without guessing. |
| BENCH-14 | Zoom form emitted 1.16 against a fixture maximum of 1.12 and dropped focal anchoring. | Owner/fixture mismatch; old proxy score invalid. |
| BENCH-15 | DEV-03 expected authored frames 120/240/360/480 while the analyzer measured 119/239/359/479. | Predicates must bind measured receipts. |
| BENCH-16 | five-track-analysis passed an audio URL to a decoded-audio analyzer and accepted a near-120-BPM fallback. | KNOWN_DEFECT in an alternate live analysis caller; not unified beat evidence. |
| BENCH-17 | Luna repair inherited roughly 14.4 seconds of a shared 40-second stage budget. | Provider timeout, not editing failure; attempts require independent declared budgets. |
| BENCH-18 | A Qwen replay dropped selected operations before compilation. | Invalid transformation; preserve raw selected operations. |
| BENCH-19 | Repair guidance disclosed evaluator-specific topology. | Diagnostic repair only; not comparable first-pass evidence. |
| BENCH-20 | Initial human-review packs were empty and only the user reviewed later outputs. | Preserve ordinal pilot results; never claim two-reviewer agreement. |
| BENCH-21 | A hand-authored mechanics render was described as though each model produced it. | Separate mechanics, model lineage and rendered editorial judgment. |
| BENCH-22 | Results across isolated stages, continuations, repairs, replays, timeouts and protocols were aggregated. | No provider winner may be inferred from those leaderboards. |

## C. Current DEV-01 through DEV-04 research hardcodes

| ID | Hardcode or assumption | Flag | Required interpretation |
| --- | --- | --- | --- |
| DEV-01 | Exact speech ranges, dead-air range, product moment, post-cut frame mapping, project revision, zoom bounds and ducking windows. | FIXTURE_FROZEN | Synthetic native-edit fixture only. It cannot establish general silence removal, product emphasis or dialogue mixing quality. |
| DEV-02 | Six-second/180-frame, 30-fps moving-panel construction; exact panel geometry, gutters, source slots, continuity samples and render thresholds. | FIXTURE_FROZEN | Generated-island mechanics and hybrid handoff fixture only. It is not a universal collage form or routing rule. |
| DEV-03 | Measured beat frames 119/239/359/479, exact cut moves and a bounded camera-shake form/proof threshold. | FIXTURE_FROZEN | Audio/video native-edit fixture only. The measured frames belong to its frozen WAV; the shake parameters require separate form-owner calibration. |
| DEV-04 | 240 frames at 30 fps, target crossing near frame 120 and expected capability gap for moving matte/rotoscoping. | FIXTURE_FROZEN | It proves the current packet must not fake the effect. It does not claim future Editron cannot implement tracking, masks or rotoscoping. |
| DEV-05 | Evaluator policy contains exact allowed operators, effect groups and required dependencies per DEV case. | RESEARCH_BOUND | This is hidden evaluator gold in v2r-semantic-operator-policy.ts, not a generic production routing or planning policy. It must never be exposed as repair hints. |
| DEV-06 | Current cohort has three routes, six route/case conditions and three repetitions. | RESEARCH_BOUND | Useful bounded reliability evidence; not production confidence or domain coverage. |
| DEV-07 | Per-case limits include 6–20 turns, 4,096 output tokens per turn, 64,000 input tokens, two identical calls and task-specific callable subsets. | RESEARCH_BOUND | Fair-run controls, not product conversation or edit-complexity limits. |
| DEV-08 | Live transport defaults to 240 seconds, up to three transient attempts and a 60-second retry-delay ceiling. | PROVISIONAL_POLICY | Operational safety defaults that need provider-specific latency/retry measurement before production SLOs. |
| DEV-09 | OpenAI token preflight uses local o200k estimation plus 15 percent and 512 tokens; Gemini uses official countTokens plus the same margin. | PROVISIONAL_POLICY | A conservative benchmark estimate, not proof of a true provider worst-case bound. |
| DEV-10 | Current CAP-2A tool dossier contains forty operations. | RESEARCH_BOUND | It is the complete relevant research packet for these tasks, not all tools in Editron and not the Adobe-class destination. |
| DEV-11 | V27 Luna and Terra results cover eighteen narrow expected outcomes each. | RESEARCH_BOUND | The 17/18 rows support continuing research. They do not select a production editorial model or certify real-project mutation. |
| DEV-12 | Gemini HTTP 429 rows were sometimes discussed as failure. | HISTORICAL_INVALID_EVIDENCE | Current disposition is PROVIDER_INFRASTRUCTURE_UNVERIFIABLE until actual model output exists. |
| DEV-13 | Qwen was removed from future cohorts. | USER_DECISION | Historical evidence remains. No technical conclusion that Qwen is globally worse may be inferred from retirement. |
| DEV-14 | `development-cohort-routes-v2.ts` preserves issued Luna/Terra price values of $1/$6 and $2.50/$15 per million input/output tokens, while the current official snapshot used by V2R-3 is $0.20/$1.20 and $2/$12. | HISTORICAL_ISSUED_SNAPSHOT | Do not silently rewrite historical experiment identities. New runs must use the dated V2R-3 route facts; historical cost ceilings and estimates must be interpreted using their own issued price table. |
| DEV-15 | The tracked generated-composition smoke harness retained a 90-second wall/60-second CPU ceiling after the frozen DEV-02 program and connected Stage 6 path had moved to 180/120 seconds. | FIXED_CURRENT_HISTORICAL_INVALID_EVIDENCE | The old ceiling rejected the current renderer as `RESOURCE_BUDGET_EXCEEDED`. Using the unchanged program-owned limits, current production snapshot `snap_CRyxD1...` rendered in 94,057 ms under host receipt `73291ed9...`, with hash-bound outputs and zero project mutation. This requalifies the sandbox mechanics only; the old V1 capability identity remains immutable and the connected route still requires a new versioned capability before execution. |
| DEV-16 | One current V2 connected DEV-02 run rendered the 180-frame generated island and joined it to a native continuation as a 345-frame H.264 proxy. | RESEARCH_PROVEN_LIMITED | Source receipt `9425dd46...`, hybrid receipt `1a56ecb4...` and output SHA-256 `aa5f6644...` pass frame integrity, panel geometry, title form, opposed motion, phase structure, full-canvas release, boundary continuity and native continuation with zero project reads/writes/state effects. Creative taste and flash safety remain `UNVERIFIABLE`; the output has no audio and is fixed 1080x1920/30/1. This is executable research mechanics, not product nested-composition support or a native/generated/hybrid routing verdict. |

## C2. Current sealed-holdout benchmark hardcodes and evidence debt

| ID | Hardcode or assumption | Flag | Required interpretation |
| --- | --- | --- | --- |
| SEALED-01 | The unseen cohort contains eight synthetic tasks, sixteen opaque cases, forty operation records, thirty-three callable operations and seven visible unavailable operations. | RESEARCH_BOUND | This is a leakage-controlled Stage 2.5 sample, not the complete Editron toolset, Adobe-class coverage or production confidence. |
| SEALED-02 | Operation presentation uses one deterministic case-bound order shared across providers and direct/opaque arms. | RESEARCH_BOUND | It prevents a model-specific ordering advantage in this cohort. It does not prove robustness to every catalog size or ordering; later trials must vary order under a new frozen identity. |
| SEALED-03 | Initial provider inputs are bounded at 85,000 tokens after measured maxima of 75,011 for Luna/Terra and 81,464 for Gemini. | RESEARCH_BOUND | The ceiling was derived for these 96 exact initial requests. It is not a provider limit, a product conversation limit or evidence that later turns fit. |
| SEALED-04 | OpenAI input size uses local o200k estimation plus 15 percent and 512 tokens; Gemini serializes the request into official `countTokens` and applies the same margin. | PROVISIONAL_POLICY | Both are conservative benchmark accounting policies, not exact billing or guaranteed context-window upper bounds. Gemini counting is provider-context egress and must remain separately authorized and logged. |
| SEALED-05 | Historical V2R-2 fixes 24 turns, 4,096 output tokens per turn and two identical calls. Budgeted V2R-3 instead derives selected-operation, per-operation candidate and cumulative-output limits from each public case, permits only `maxNodes + 2 schema repairs + 1 finish` turns, and dynamically lowers the next output allowance. | IMPLEMENTED_NOT_DISPATCH_PROVEN | The historical identity remains immutable and unguarded by design. Only V2R-3 is eligible for a future sealed run. Its candidate accounting is explicitly pinned to catalog V2R-8 search/resolver outputs and must be reviewed on every catalog revision. A complete zero-inference simulation is still required before dispatch. |
| SEALED-06 | The credential preflight bounded initial requests only. V2R-3 now requires a request-hash-bound per-turn input upper bound, route/case/manifest authorization, worst-case pre-invoke spend reservation and exact post-response usage accounting. | IMPLEMENTED_NOT_DISPATCH_PROVEN | OpenAI cache-read/cache-write/output and Gemini cached-input/output/thought tokens are separately accounted. Missing, forged, inconsistent or over-bound usage fails closed before owner execution. A real runner must still supply the approved per-route counter and pricing facts and prove all turns in the zero-inference simulation. |
| SEALED-07 | Task fixtures declare synthetic/no-egress policies, while a separate operator authorization allowed three metadata GETs and thirty-two Google `countTokens` POSTs. | RESEARCH_BOUND | Task policy is not itself provider authorization. The receipt must preserve this distinction; a future inference run needs an explicit egress/spend authorization covering its exact request hashes and routes. |
| SEALED-08 | Complete tool context is repeated across the prompt, tool authority and provider declarations, producing roughly 67k-81k bounded initial tokens. | PROVISIONAL_POLICY | The repetition is transparent but expensive. Stable-prefix caching or a smaller lossless representation may be benchmarked only if request semantics remain byte-auditable and every provider receives equivalent information. |
| SEALED-09 | The connected owner records typed operations and writer revisions in an isolated in-memory log. | RESEARCH_BOUND | It validates protocol and causal handoff only. It is not real native execution, generated compilation, ProjectService mutation, render proof or editorial quality evidence. |
| SEALED-10 | Runtime budgeting and its budgeted trace/evaluator landed as two clean commits: `ddfc04b78` and `4d6230a04`. | VERIFIED_CURRENT | V2R-3 binds the runtime authorization, per-turn accounting and resource receipt through a separately versioned lossless trace and evaluator. Historical V2R-2/V1 artifacts remain unchanged. At this checkpoint, proof adapters and complete zero-inference accounting-plus-proof simulation remain mandatory before dispatch; SEALED-11 records the subsequently implemented standalone route binding. |
| SEALED-11 | Commit `94de011ae` binds official dated route prices, the exact manifest/case/public-case/route approval, local OpenAI estimation and separately authorized Google `countTokens` context egress while authorizing zero inference. | IMPLEMENTED_NOT_EPISODE_PROVEN | Route/request/approval/expiry/counter drift fails closed and no secret enters the receipt. This closes only the standalone route-accounting boundary. Claim-appropriate proof adapters and a complete zero-inference episode simulation remain mandatory before dispatch. |
| SEALED-12 | Commit `83405595b` proves the research-isolated no-edit outcomes for HOLD-06/07/08 from a recomputed hidden evaluation, accounted trace, allowed terminal and zero successful mutation/generated nodes. | RESEARCH_PROVEN_LIMITED | This proves safe non-execution inside the sealed research authority. It explicitly does not prove real ProjectService byte equality, product mutation safety or render quality. HOLD-03 still needs generated/hybrid proof; SEALED-13/14/15/16 record later bounded HOLD-01/02/04/05 proofs. |
| SEALED-13 | Commit `3e22af490` renders HOLD-01 from the model-selected `use_matching_footage` source start and proves decoded adjacent-frame geometry against fixed `0.03` centre-distance and `0.9..1.1` diameter-ratio thresholds. | RESEARCH_PROVEN_LIMITED | The thresholds come from the frozen HOLD-01 rubric and the adapter is intentionally restricted to one 640x360, 30/1, H.264 source-range splice. It rejects altered media and a visibly wrong later source start, but it does not prove ProjectService mutation, product-renderer parity, mixed rates, arbitrary crop/keyframe plans or general match-cut certification. |
| SEALED-14 | Commit `81eeb933a` renders the three model-selected HOLD-02 ranges and measures decoded door-open, process and door-close action while binding opaque writer-revision handoff. | RESEARCH_PROVEN_LIMITED | It proves only `[0,240)` of a 720-frame fixed-30-fps synthetic project. The remaining range, ProjectService mutation, product renderer, mixed rates and general repeated-footage taste are not proved. A semantically wrong but structurally distinct closing range is deliberately rejected after pre-proof. |
| SEALED-15 | Commit `82f412bbb` binds HOLD-04's one selected cut to the real `cutTimelineRange` owner, proves one retained caption occurrence and unchanged computed presentation material, and decodes the resulting H.264/AAC proxy. | RESEARCH_PROVEN_LIMITED | The fixture is fixed 30/1 with synthetic tone and only a symbolic caption presentation reference. Caption pixels, intelligible speech, real ProjectService mutation/save/reload/undo, product-renderer parity and mixed rates are explicitly not proved. |
| SEALED-16 | The historical v1 `buildSubjectAwareReframePlan` treated normalized subject centres as CSS `object-position` percentages and left skipped overlays in old absolute pixels after a canvas change. Commit `0404a253f` replaces that behavior with source-raster-bound, box-aware cover geometry and explicit authored-layout projection in the same sole owner; commit `8d1afc89f` binds that owner to the sealed H05 trace and media. | RESEARCH_PROVEN_LIMITED | The former owner clipped the bound H05 subject on 38/450 frames (worst 360x640 margin `-20.53px`). The new adapter decodes all 450 source frames and all 450 360x640 H.264 output frames; a static crop and changed media fail closed. It proves a symbolic top-right-five-percent marker, not real logo pixels, ProjectService mutation, live-chat safety, audio preservation, mixed rates or general reframe certification. CAP-2A V3 remains immutable history and explicitly reports current-source drift until reissued after HOLD-03 stabilizes. |
| SEALED-17 | The paid V2R2/V2R3 cohort executed all 96 rows: 466 provider turns, 176 Google `countTokens` calls, `$9.730960595` recorded spend and zero project reads/mutations. | VERIFIED_CURRENT | Raw cohort receipt is `9582dffc068b7319835d48da4834c1de70bdb29b39aa011ce0239fa12238022f`. This proves execution/accounting/resume only. It is not a 96-row pass, model ranking or production result. |
| SEALED-18 | Runner status `PASS_CLAIM_PROOF` combines safe no-edit proof with rendered edit proof. | KNOWN_DEFECT | All 24 raw passes in this cohort are the general no-edit safety proof; there are zero original rendered-edit passes. Commit `b5f2af0da` adds a separate immutable interpretation owner. A future runner identity must use semantically distinct raw statuses. |
| SEALED-19 | HOLD-01's visible resolver can propose `cut_section`, while hidden proof accepts only `use_matching_footage`. | HISTORICAL_INVALID_EVIDENCE | Five non-resource HOLD-01 failures cannot rank the models. Freeze a new identity only after one declared operation contract is shared by owner, callable schema and proof. |
| SEALED-20 | Two HOLD-02 proof-eligible rows failed because the Windows output path was about 266 characters. | HISTORICAL_INVALID_EVIDENCE | Unchanged row 013 and 023 traces pass the frozen rendered proof under a short path. Reproof receipts are `7b9e96a3...` and `13097e0f...`. Future proof roots must be bounded before FFmpeg runs. |
| SEALED-21 | HOLD-03 proof requires a reference literal different from the visible owner binding, receives only a generic `layoutSpec` schema and renders a human-authored program fixture. | HISTORICAL_INVALID_EVIDENCE | Five C1 execution rows are confounded. The next identity must expose exact protected literals and typed nested inputs, then bind proof to the model-generated program rather than substituting the mechanics fixture. |
| SEALED-22 | HOLD-04's cut owner retimes captions, but the research episode does not expose truthful post-cut caption/timeline state or declare that owner effect adequately to the model. | HISTORICAL_INVALID_EVIDENCE | Four non-resource execution failures are confounded. The next owner session must maintain and return an evolving clone plus the exact cut receipt/coordinate and declared state effects. |
| SEALED-23 | HOLD-05 proof requires exact nested reframe fields while the callable `reframePlan` is only a generic object. | HISTORICAL_INVALID_EVIDENCE | Five C1 execution failures cannot rank the models. Publish a complete schema or return a typed opaque resolver result that proof consumes without model retyping. |
| SEALED-24 | Thirty rows stopped at the conservative 85,000-token input guard; opaque handoff stopped 23/48 versus direct 7/48. | RESEARCH_BOUND | These rows are `NOT_EVALUATED_RESOURCE_GUARD`, not model failures. A new identity must separate a capability-ceiling condition from a production-budget condition so ability and unit-economics are measured independently. |
| SEALED-25 | The first current V4R evaluator treated any successful visual retrieval plus any successful native mutation as sufficient for `READY_FOR_PROOF`. | FIXED_CURRENT_HISTORICAL_INVALID_EVIDENCE | This admitted an unsupported HOLD-01:C2 range deletion after noisy evidence. Commit `ccbe5fc2d` binds the current evaluator to the shared proof-owner allowlist, forbids H01 range deletion and requires the matching-footage owner for H01:C1. Partial `v4r-run-04` rows are defect evidence only. Historical evaluator identities remain unchanged. |
| SEALED-26 | V4R has one row per provider/case, balanced but different direct/opaque handoff and tool-order assignments. | RESEARCH_BOUND | Luna's 7/15 and Terra's 6/15 proof-pass counts are descriptive current-context outcomes, not reliability estimates or a leaderboard. Repeated paired trials are required for statistical model selection. |
| SEALED-27 | HOLD-02 proof is frozen to an exact 240-frame synthetic door/process/door sequence. | FIXTURE_FROZEN | The 720-frame and 360-frame alternatives selected in the current run are genuine failures against this case, not evidence that their general editing taste is universally wrong. The fixture does not certify general repeated-footage policy. |
| SEALED-28 | All fifteen current Gemini 3.7 rows terminated on HTTP 429 before usable model output. | PROVIDER_INFRASTRUCTURE_UNVERIFIABLE | They are neither passes nor model failures. Do not substitute Gemini 3.6 into the V4R identity or compare 0/15 with Luna/Terra. A new versioned supplement requires a callable funded route. |
| SEALED-29 | Reopening the completed V4R run root returned the identical cohort receipt without creating another row or attempt. | RESEARCH_PROVEN_LIMITED | This proves bounded local artifact reuse/resume for a completed cohort. It does not prove mid-episode context compaction/resume, durable production workflow recovery or recovery after a project revision conflict. |
| SEALED-30 | The V3R3 visual supplement maps episode assets `clip-a`, `clip-b`, `clip-c` and `product` into the owned `dev03-cards` fixture with source offsets `0/117/267/450`; it renders 640x360 at 30/1 for 720 frames and samples boundaries 118/119 and 238/239. Its fixed acceptance thresholds are boundary mean-absolute-difference at least 20, filter difference at least 2, scale ratios 1.05..1.12, horizontal centre shift -25..-4, vertical shift -3..3, and a fixture-specific cream-pixel detector. | RESEARCH_PROVEN_LIMITED | Portable receipt `62a1fb24...` proves those exact synthetic pixels and the stored Luna P1 operation result only. Offsets, raster, rate, sample frames, colour detector and thresholds are frozen proof-fixture policy; none may become general media mapping, timebase, filter, zoom, focal or quality policy. |
| SEALED-31 | The V3R3 rendered supplement covers one Luna P1 row and the source episode contains no audio overlay or ProjectService persistence. | RESEARCH_PROVEN_LIMITED | Isolated owner replay and rendered visual evidence pass. Rendered audio and ProjectService reload remain explicitly unverifiable; the receipt cannot promote the six-operation set, Luna, CAP-2A or Stage 2.5 to production. |
| SEALED-32 | CAP-2A V6 still represented current source truth and H03 generated-source V3R4 could be dispatched again after the product-root work. | SUPERSEDED_IDENTITY | False for current dispatch, but not for historical execution. Fresh extraction found one new signed route, eight new durable worker/job modules and eleven changed evidence paths. Commit `3703c0815` preserves V6 and issues V7: 231 paths, 486 observed identifier occurrences, normalized snapshot `d476471b...`, manifest `939ec670...`, 37 declared operations and zero certified/product-eligible operations. H03 generated-source V3R4 already ran at `7064fe381`: receipt `47a57bf2...` binds 18 rows, 4 rendered passes, 8 proof-unverifiable rows, 6 Gemini-429 non-evaluations, `$0.21068595` spend and zero project access. That run remains bounded historical evidence. Its current constructor is correctly non-dispatchable because it binds V6 and reconstructs a historical base from mutable builders. A current-source rerun requires a new V7-bound identity with a committed pinned base; never patch or redispatch V3R4. Native handoff/order V3R4 is a separate 18/18 bounded experiment and must not be conflated with this H03 cohort. |

## D. Production timebase, media and render assumptions

| ID | Current hardcode or missing identity | Flag | Code-grounded consequence |
| --- | --- | --- | --- |
| TIME-01 | Main editor constant FPS equals 30; React editor reads/writes that constant. | KNOWN_DEFECT | components/editron/editor/version-7.0.0/constants.ts line 7 and react-video-editor.tsx lines 108, 183 and 306 make Project.fps look more configurable than the active editor path is. |
| TIME-02 | Project creation defaults to 30 and one save path falls back with state.fps or 30. | KNOWN_DEFECT | project-service.ts lines 419, 472, 516 and 1664 preserve the 30-fps assumption across storage. |
| TIME-03 | Remotion metadata coerces fps through a rounded positive integer. | KNOWN_DEFECT | remotion/metadata.ts cannot preserve 24000/1001, 30000/1001 or 60000/1001 exactly. |
| TIME-04 | Many UI, captions, overlays, MG and five-track conversions multiply or divide by 30. | KNOWN_DEFECT | Mixed rates and even non-30 integer project rates can produce wrong timing. The family owners must migrate deliberately; a blind global replacement is unsafe. |
| TIME-05 | Chapter thresholds are 27,000/4,500/900 frames while the fps parameter is unused. | KNOWN_DEFECT | Intended 15-minute, 150-second and 30-second durations change at other rates. Source: chapter-renderer.ts lines 35–46 and 111. |
| TIME-06 | `MediaAsset` can now hold a stable complete-byte `sourceVersionV1` for the signed-registration path, but it still lacks exact stream cadence/timebase/PTS/VFR mapping, source timecode/reel, SAR, field order and full pixel/colour identity. | MISSING_CONTRACT | Byte identity does not supply source-frame identity, mixed-rate conform, professional relink, HDR/log or deterministic delivery proof. |
| TIME-07 | Current upload path verifies duration and stores rounded dimensions, not the complete stream identity above. | MISSING_CONTRACT | Browser/editor range coordinates cannot be treated as source-frame identity. |
| TIME-08 | Current analysis paths use caps such as 120 or 300 seconds, one-fps sampling, 64 frames per V-JEPA segment and 360 visual segments. | PROVISIONAL_POLICY | These can support baseline search/structure but not exact advanced edits unless claim-conditioned dense inspection is implemented and persisted. |
| TIME-09 | Current UI has a small set of 1080-long-edge aspect presets, while other defaults still use 1280x720. | KNOWN_DEFECT | There is no one certified raster/format authority. The presence of a dimension field is not DCI, UHD, HDR or delivery certification. |
| TIME-10 | Main delivery paths use H.264/AAC and inconsistent colour declaration between paths. | MISSING_CONTRACT | A rendered MP4 is not professional codec, colour, audio-layout, mastering or interchange certification. |
| TIME-11 | The only defensible current main-path compatibility claim is approximately 30/1 progressive, square-pixel SDR around current web presets, pending golden output certification. | VERIFIED_CURRENT | Do not claim system-wide 24/25/29.97/50/59.94/60, VFR, drop-frame, DCI, HDR, high bit depth, mixed rates or source timecode. |
| TIME-12 | Four-, five- and ten-hour projects are product scalability targets. | PROVISIONAL_POLICY | They are not separate presets and are not currently certified. Numeric frame count is not the blocker; source identity, bounded decode, virtualization, sharding, resume and proof are. |
| TIME-13 | `FFPROBE_BEST_EFFORT_TIMESTAMP` is sufficient as a source-coordinate or camera-timecode truth. | PROVISIONAL_POLICY | Commit `426d3d09a` permits it only as an explicitly named decoder-derived candidate origin inside a pure local-shard verifier. It is not camera timecode, a complete presentation map, a proxy transform or project-timeline truth. Promotion requires a full qualified mapping policy and direct calibration evidence. |

## E. Project authority, revision, proof and security assumptions

| ID | Claim or condition | Flag | Current truth |
| --- | --- | --- | --- |
| AUTH-01 | IF1 was sometimes discussed as though frozen meant active. | HISTORICAL_INVALID_EVIDENCE | The tagged accepted commit remains historical, but `d1402ff38` restores its reviewed five-file vocabulary artifact to active HEAD. It remains inert: no product runtime imports it, ProjectService has not implemented its issuer boundary, and the product has not migrated to it. |
| AUTH-02 | The active ProjectMutationReceiptV1 was treated as close to IF1. | MISSING_CONTRACT | It contains projectId, revision and committedAt only. It lacks canonical command hash, timeline revision, changed paths, proof, undo/replay and full retry disposition. |
| AUTH-03 | “Rollback race closed” was stated too broadly. | HISTORICAL_INVALID_EVIDENCE | Writer-issued after-revision closes the exact migrated race only. Before checkpoints can pair a caller snapshot with a separately read revision; after checkpoints can load newer state and bind it to the earlier writer receipt. |
| AUTH-04 | Whole-state and generic writers were assumed safe because ProjectService exists. | KNOWN_DEFECT | `saveProject` can still be called without a caller-supplied expected revision. Commit `7b190ae90` removes chat subject reframe from the raw-writer class: it loads one ProjectService mutation snapshot and persists editor state plus its audit path through one `saveProjectWithReceipt(expectedRevision, projectUpdates)` CAS. It remains a whole-project writer with no range receipt, rebase, undo/replay or render proof. Commit `d8e61f060` removes the raw generic duration seam: `updateProject` now rejects arbitrary fields and delegates its one permitted `durationInFrames` assertion to `reconcileProjectDurationFromOverlaysV1`, which derives current overlay extent and performs a receipt-bearing CAS. The two direct callers (`agent/tools.ts`, `auto-edit-service.ts`) still use the compatibility bridge with `UNKNOWN_LEGACY_CALLER`, drop the receipt, and do not make Auto Edit's multi-write sequence atomic. The duration receipt also conservatively blocks cut rebase until an explicit policy models it. |
| AUTH-05 | Existing checkpoint/undo work was treated as replay-complete. | MISSING_CONTRACT | Safe paths exist, but complete IF1 undo/redo/replay and project-wide writer migration do not. |
| AUTH-06 | P0 worker hardening commit 5299a42 was discussed as though active. | HISTORICAL_INVALID_EVIDENCE | It is in a separate worktree and not an ancestor of this branch. That historical claim is not the current basis for protection: active Video Analysis, TRIBE and automatic Director routes now use request-time `withInternalQStashWorkerAuth(...)`, and their production downstream-dispatch configuration fails closed before handler/database work. This does not certify every worker route, deployment configuration or remaining project writer. |
| AUTH-07 | Generated composition research rendering was discussed as product execution. | HISTORICAL_INVALID_EVIDENCE | ProjectService-owned generated-composition state foundations exist, but the active editor/renderer product path is not end-to-end wired and certified. Research sandbox execution is not a second project authority. |
| AUTH-08 | A production agent control plane, PlanService and generalized PreviewObservationService were sometimes described as one converged runtime. | PARTIAL_CONVERGENCE | Immutable PlanService contracts/store, accepted-node job binding, durable lifecycle/recovery and a bounded long-form proposal compiler now exist. Commit `7319da514` proves structural compilation only. Canonical proposal apply/reload, product-issued change regions/transforms/locks, dirty-range observation caching and a generalized PASS/FAIL/UNVERIFIABLE preview-proof service are not converged. |
| AUTH-09 | Supplying `Authorization: Token <MODAL_TOKEN_ID>:<MODAL_TOKEN_SECRET>` authenticates a Modal web endpoint. | SOURCE_CLOSED_DEPLOYMENT_UNVERIFIED | Modal web functions are public unless their declaration requires proxy authentication or a proven manual check exists. Commits `025b4e73b`, `b96253b52` and `27dd95b83` correct the non-deployed source-probe pair; `eb7836dc9` corrects Scene Detection; `74f66518b` corrects Music Analysis; `f0ccf4dbe` corrects Wav2Vec; `d8ff0fbc6` corrects V-JEPA; and `436b37e19` corrects the standalone Gemma editorial classifier with static deployment/client guards and dedicated proxy credentials. The source sweep separately classifies Editorial Plan's signed-handler factory, the scoped MG storage capability callback, and manually bearer-authenticated finalizer/concat/Brand-Vault services. No Modal deployment or environment state was changed or verified; manual bearer endpoint-origin/rotation policy and storage URL provenance remain open. |
| AUTH-10 | A canonical reference registration receipt proves that the temporary `videoUrl` or `audioUrl` passed to every analyzer is storage-owner-issued and safe to fetch. | KNOWN_DEFECT | `assertCanonicalReferenceAnalysisSourceV1` validates registration identity but does not bind `source.videoUrl` to that receipt. Scene Detection, Music Analysis, Wav2Vec and V-JEPA now require proxy auth, which limits who can invoke them, but their workers still fetch caller-provided temporary URLs. Before production deployment, the canonical media source owner must issue/bind the URL or opaque handle and enforce origin, redirect, byte-size and expiry policy; do not create an analyzer-specific media registry or ProjectService writer. |
| AUTH-11 | Failure/cancellation cleanup of a Director lease can clear a newer lease or leave no revision/receipt for following observers. | REMEDIATED_NARROWLY | Commit `a20f052a9` changes `ProjectService.releaseDirectorMutationLease` from a raw token-only unset to an atomic active-token writer: successful release clears the lease, advances `projectRevision` and `updatedAt`, and publishes/returns `ProjectMutationReceiptV1`; old or absent tokens return a no-write disposition. This is not generic range locking, safe rebase, undo/replay, timeline effects or render proof. The current Director cleanup caller does not yet surface the returned receipt, and separately audited Director observer-fact writers remain open. |

## F. Architecture and product hypotheses still requiring proof

| ID | Hypothesis | Flag | Required test before production use |
| --- | --- | --- | --- |
| ARCH-01 | A model can reconstruct global editorial language, recurring grammar, hero moments and protected literals from references. | PROVISIONAL_POLICY | Multimodal heldouts with native video/audio, dense annotations, missed-event scoring, uncertainty calibration and blind editor judgment. HREF-01 is only the sparse first arm. |
| ARCH-02 | A model can create a useful Sequence/Range plan and choose the next edit in a durable agent loop. | PROVISIONAL_POLICY | Commit `7319da514` establishes a 9/9 deterministic structural gate for one 4.5-hour evidence directory. Commit `4a0538847` adds provider-episode evaluation through the same PlanService compiler, but still proves only structural validity on synthetic receipts. The earlier live preflight predates this evaluator and cannot authorize dispatch. Real provider proposals, editorial quality, source-range semantic accuracy, whole-episode exact tools/results, replan, conflict, compaction/resume, proof and user correction time remain untested. |
| ARCH-03 | Native/generated/hybrid routing can be decided by target coverage, certified ownership, relationships, editability, sandboxability and proof. | PROVISIONAL_POLICY | Force all legal routes on held-out tasks and compare fidelity, defects, editor correction time, editability, round-trip preservation, latency and cost. Never route by operation count alone. |
| ARCH-04 | Versioned evidence policies can determine enough evidence for a target claim and candidate operation. | PROVISIONAL_POLICY | Per-operation/claim policies need expert-authored minimums, negative cases, analyzer-version bindings, dense-ground-truth recall and rendered proof calibration. There is no universal automatic policy writer. |
| ARCH-05 | Learned masks/tracks such as SAM 2 or CoTracker outputs can support masking/rotoscoping. | PROVISIONAL_POLICY | They are candidate measurements, not proof. Require identity, drift, occlusion, edge and rendered-composite validation. |
| ARCH-06 | A small repair loop can make generated compositions production reliable. | PROVISIONAL_POLICY | Measure zero, one, two, four and eight-repair curves by task class. Do not assume 1–3 or copy a research result that allowed up to 50. |
| ARCH-07 | A legal internal caption preference set can improve taste. | PROVISIONAL_POLICY | Rights-cleared rendered alternatives, editor/client pairwise judgments, accessibility and brand coverage, held-out regression measurement and explicit override. It is not an open-dataset taste oracle. |
| ARCH-08 | FSD50K plus model intelligence can become a production SFX system. | PROVISIONAL_POLICY | Rights/provenance, catalog coverage, event extraction, retrieval, human-listening ground truth, mix proof and sequence-level repetition/density evaluation remain required. The 11-item S2 pilot is tooling calibration, not catalog sufficiency. |
| ARCH-09 | Reusing a vibe-coding shell will materially accelerate a web-native vibe editor. | PROVISIONAL_POLICY | Bounded spike must compare existing Mongo/QStash jobs, a durable workflow layer and selected shell components while keeping ProjectService/plan/media authority in Editron. |
| ARCH-10 | Cheap client capture plus deterministic server observation is sufficient. | PROVISIONAL_POLICY | Compare state diffs, browser frames, server stills, ordered motion proxies, audio windows and sequence previews for parity, latency, cost, reproducibility and claim coverage before rollout. |

## G. Current HREF-01 human review gate

The exact item-by-item checklist is maintained in
[the HREF-01 no-spend preflight](./oe-v2r-heldout-reference-01-preflight-2026-08-20.md#human-rubric-review).
Human approval confirms only that the frozen evaluator notices reasonable
things about this one source before model responses are seen. It does not
approve fourteen frames as a production sampling policy and does not replace
the later two-editor moving-reference pilot.

## H. Resolution rules

1. A FIXTURE_FROZEN value stays immutable for that experiment version. A better
   fixture receives a new identity; historical inputs and results are preserved.
2. A RESEARCH_BOUND or PROVISIONAL_POLICY value cannot silently become a product
   default. Promotion requires a declared scorecard, heldout evidence and owner.
3. A KNOWN_DEFECT must remain visible in the master ledger until active control
   flow and proof show it closed.
4. A MISSING_CONTRACT cannot be filled by a model description, shared helper,
   passing unit test or one successful render.
5. HISTORICAL_INVALID_EVIDENCE may be useful diagnostically but may not
   contribute to provider ranking, routing or production claims.
6. Future status reports must cite this register when they rely on any listed
   constant, threshold, capability or architecture assumption.

## I. Reference materialization and storage assumptions

| ID | Hardcode or assumption | Flag | Current evidence and required disposition |
| --- | --- | --- | --- |
| MEDIA-01 | `uploadFileToR2` selects one streaming PUT at or below 64 MiB and multipart ranges above 64 MiB. | PROVISIONAL_INFRASTRUCTURE_POLICY | Commit `219c0e7ab` proves bounded streams, multipart completion/abort and backend failure behavior. The split is an operational memory/retry choice, not a content duration, format, quality, account-tier or compatibility limit. Calibrate it with real R2 latency, retry, memory and cost telemetry before treating it as production policy. |
| MEDIA-02 | The file-stream upload/registration boundaries are present, therefore long-form reference ingest is streaming end to end. | MISSING_WIRING | False today. Commits `7855aa90e`, `0917a6c4e` and `849ecc8a5` stream the default remote canonical source, demux source/video/audio files, remote frame-sampler input and derived JPEG hashing/upload/registration. The SaaS request-upload route still starts with `file.arrayBuffer()` and deliberately passes a Buffer downstream; other unrelated media consumers also retain byte-based paths. These commits prove specific boundaries, not every ingress/caller or universal long-form compatibility. |
| MEDIA-03 | A successful file upload is a canonical media-registration receipt. | PARTIAL_CONVERGENCE | Still false as a general rule. Commit `54273324d` makes this specific demux owner register exact content-addressed VIDEO/AUDIO children and reject mismatched receipts; `a70a37158` binds the final/core/child-registration receipt hashes into newly issued V2 source envelopes. Upload and registration remain separate actions, partial failure can leave an object/row requiring reconciliation, source registration does not re-fetch arbitrary receipt bodies or live object bytes, and historical V1 remains read-compatible. Never treat an R2/GCS object or a syntactically valid hash alone as canonical evidence. |
| MEDIA-04 | Pre/post file size and `mtimeMs` plus a streamed SHA-256 fully prove that the uploaded object contains those exact bytes. | PROVISIONAL_INFRASTRUCTURE_POLICY | Commit `a8b7036f9` detects ordinary local mutation during hashing and issues exact local-byte identity. It does not cryptographically compare live R2/GCS object bytes, and upload and registration are not one atomic action. The production caller must use an immutable private file, bind upload and registration to one content identity, clean up an orphan on failure, and obtain live object/store proof before promotion. |
| MEDIA-05 | Removing the canonicalizer's internal 90-second timeout makes remote reference acquisition durably cancellable. | MISSING_WIRING | False. Commit `7855aa90e` accepts an optional `AbortSignal`, eliminating a hidden whole-download duration cap, but not every live caller propagates a durable job cancellation or deadline. Commit `f5b192f2c` composes the product episode root but does not yet bind durable cancellation/deadline state into canonical fetch and cleanup. That binding and live interruption proof remain required before this is considered hang-safe. |
| MEDIA-06 | All reference intake now avoids holding the full uploaded source in application memory. | MISSING_WIRING | False. The default remote fetch is streamed, but the current SaaS upload path already receives a request Buffer and deliberately supplies `downloadSourceBytes`. Long-form upload ingress, request-body streaming and every caller's memory envelope remain uncertified. |
| MEDIA-07 | AAC 192 kbit/s is a universally suitable canonical demux-audio representation. | PROVISIONAL_MEDIA_POLICY | False. `reference-demux.ts` inherits `192k` AAC from the YouTube importer as an analysis artifact policy. It is not preservation of the original audio stream, a mastering format, or a certified choice for every channel layout/sample rate/content class. Preserve immutable source identity separately and benchmark an explicit analysis-proxy audio contract before promotion. |
| MEDIA-08 | Hashing the final demux receipt directly is sufficient provenance for registrations embedded in that receipt. | INVALID_CIRCULAR_IDENTITY | False: the registration rows must exist before their receipt hashes can appear in the final receipt. Commit `54273324d` resolves this with a deterministic core receipt that excludes creation time, temp paths, storage and registration receipts; children bind the core hash, then the V2 final receipt binds child registration hashes. Do not collapse the two identities or add volatile fields to the core. |
| MEDIA-09 | A 350-MiB remote-source ceiling plus `scale=1280:-2`/JPEG quality 3 is a production reference-analysis standard. | PROVISIONAL_MEDIA_POLICY | False. Commit `849ecc8a5` preserves these pre-existing frame-sampler values while removing whole-file memory allocation; it does not certify them for long-form, tiny typography, masks, tracking, grading, HDR or every content class. Replace the universal ceiling with job/storage/resource policy, and version/certify precision-evidence recipes by target claim before promotion. |
| MEDIA-10 | A successful bounded worker byte hash means all existing Editron media and long-form workloads now have durable immutable identity. | PARTIAL_CONVERGENCE | False. Commit `278daa367` streams and SHA-256-hashes the server-minted object only on the signed-registration qualification worker, requires unchanged provider observations plus a measured technical probe, and writes the result on the existing `MEDIA_ASSETS` record. Commits `699633aee` and `a049fa1fa` make the proxy-to-master storage switch server-owned, preserve only validated proxy identity, and later materialize a master relation after a fresh qualification. Direct/server uploads, resumable hashing, source PTS mapping, timebase extraction, invalidation consumers, ProjectService source bindings and deployment proof remain open. The route currently has `maxDuration = 180`. |
| MEDIA-11 | A persisted proxy/master relation and invalidation intent mean proxy replacement is production-complete. | MISSING_WIRING | False. Commit `a049fa1fa` persists `MediaProxyMasterRelationV1` only after exact proxy/master owner, asset, media-kind, R2-locator and terminal-storage checks; its mapping remains `UNQUALIFIED/SOURCE_PTS_MAPPING_REQUIRED`. The persisted `PROXY_MASTER_PROMOTED` plan is intent only: no analysis, preview, render, delivery or ProjectService consumer clears/rebases anything. Existing proxy-derived analyses or project bindings must not be treated as master-qualified until those owners and receipts exist. |
| MEDIA-12 | The local PTS/cadence descriptor’s 128-decimal-digit tick/ordinal and 256-character mapper-identity guards are media compatibility, duration or product limits. | PROVISIONAL_INFRASTRUCTURE_POLICY | Commit `426d3d09a` uses them only to bound hostile parser/resource input before `BigInt` conversion. They are not source-duration, frame-rate, container or customer limits. A future mapper must calibrate and version its resource policy independently. |
| MEDIA-13 | The `R2_PRIVATE`/`GCS_PRIVATE` sidecar types, deterministic `private/editron/media-source-pts-cadence/...` object-key shape, caller-supplied mapping lease, hash-bound `COMPLETE` receipt, guarded adapter, typed asset-state pair or Mongo CAS adapter mean a private object policy, resumable worker, asset CAS or source map is live. | MISSING_WIRING | Still false. Commits `bece283e3` and `ff27d6da6` define lifecycle/terminal envelopes; `173432a4c` adds canonical bytes plus an injectable conditional-write/read-verify R2 port; `822e9182e` adds a pure asset-state pair; `3ad3a1078` adds a source-bound CAS adapter. No mapper invokes it, no live Atlas write is proven, and there is no full-coverage verifier or terminal persistence. The object-key shape, lease, storage declaration and receipt envelope are versioned inputs, not measured evidence. |
| MEDIA-14 | The checked-in CDN worker's denial of `private/` means deployed R2 sidecars are private. | PROVISIONAL_DEPLOYMENT_POLICY | False. Commit `f7da79e32` rejects raw and encoded reserved keys before R2 access in the repository worker and its source-level test proves no R2 lookup occurs. It does not prove which worker version is deployed, which bucket is bound, whether another public route reaches that bucket, or that a future route will preserve the deny policy. Before wiring sidecar writes, verify the deployed worker identity/bucket binding and run an authenticated canary proving a reserved-key fetch returns 403. |
| MEDIA-15 | Clearing source-bound cadence state in the two `sourceVersionV1` writers proves every legacy `r2Key`/storage writer preserves or invalidates it. | MISSING_WIRING | False. Commits `228b28dd4` and `584b913ff` clear the map in proxy/master promotion and qualification claim/completion. A wider live-code inventory still includes raw storage-key writers, including `scripts/migrate-gcs-to-r2.mjs`; those paths do not necessarily participate in source-version ownership. Before any cadence-map consumer is wired, classify each writer as new-asset-only, source-preserving with exact proof, or source-replacing with atomic clearing/requalification. Do not infer safety merely because a map reader validates its own stored pair. |
| MEDIA-16 | Independently requested `ffprobe -read_intervals` time chunks begin at their requested PTS and can assign exact global frame ordinals. | INVALID_MEASUREMENT_ASSUMPTION | False. The measured tracked H.264 fixture requested at 30 seconds started at 27.833… seconds because ffprobe sought to a keyframe. The documented interval duration is evaluated after actual seeking. A production mapper must scan continuous presentation order with exact checkpoints; seek/overlap partitioning remains experimental until it proves complete coverage and ordinal equivalence. |
| MEDIA-17 | The V1 contiguous PTS shard contract represents every valid professional source. | MISSING_MODEL | False. The V1 verifier correctly fails closed on timestamp gaps, epochs or discontinuities. Those sources need an explicit versioned segment/epoch representation and verifier before support can be claimed; do not weaken contiguity merely to make a mapper finish. |
| MEDIA-18 | The V1 shard descriptor hash and rolling checkpoint make the per-frame source PTS map recoverable and fully enumerable. | PARTIAL_CONVERGENCE | Still false for a live map. Commit `785c296d7` adds a pure V2 batch codec that retains/decodes exact per-frame PTS and duration records; `923fd6fc6` adds a pure ordered V2 index with deterministic private batch keys/digests/coordinates; `3cd22d54f` verifies every batch actually listed by that index through an injected reader; `650d46b82` only promotes that result when its map binding and exact qualified source bounds agree. None persists production storage/state, runs a mapper worker, issues a terminal asset map or wires a consumer. A V2 state owner and mapper runtime are still required. |
| MEDIA-19 | The V2 batch codec's 8-MiB/100,000-record parser ceilings are media compatibility, source-duration or product limits. | PROVISIONAL_INFRASTRUCTURE_POLICY | False. Commit `785c296d7` uses those absolute ceilings only to bound hostile parser/resource input; each batch also names a lower mapper-policy-bound envelope. They do not cap a source, project, delivery or long-form workflow. Calibrate/version worker memory, batch size, storage and retry policy with real media before promotion. |
| MEDIA-20 | The V2 manifest index's 8-MiB/100,000-entry parser ceilings or deterministic object-key shape establish a scalable private storage deployment. | PROVISIONAL_INFRASTRUCTURE_POLICY | False. Commit `923fd6fc6` uses the ceilings only to bound hostile index input; per-index policy is explicitly named and must be calibrated with real media. Its `private/.../v2/...` keys are deterministic input to a future reader/writer, not proof of a deployed private bucket, object existence, mapper capacity, source-duration support or product eligibility. |
| MEDIA-21 | A verified V2 manifest index proves complete qualified-source coverage or source-wide CFR/VFR. | PARTIAL_CONVERGENCE | Still false by itself. Commit `650d46b82` supplies the missing pure qualified-observation start/end and map-binding comparison over `3cd22d54f`'s injected-reader result; partial ranges, forged coverage and cross-map indexes fail closed. No storage-owner reader, V2 state owner or terminal state write exists, so this is not an observed production source-wide conclusion. |
| MEDIA-22 | A pure source-coverage result from an injected reader proves a deployed resumable mapper or long-form product support. | MISSING_WIRING | False. `650d46b82` has no storage backend, continuous scanner, persistent checkpoint, asset-state transition, worker dispatch/retry, deployment proof, ProjectService source binding or renderer consumer. It is a prerequisite contract for those owners, not their substitute. |
| MEDIA-23 | The exact-render R2 adapter's 4.995-GiB maximum, one-hour default lease and seven-day maximum lease are universal content-duration or render-SLO policy. | PROVISIONAL_INFRASTRUCTURE_POLICY | False. Commit `154dfd74f` deliberately limits its conditional single PUT to Cloudflare's documented single-upload ceiling and its signed GET to the provider's documented 1-second-to-7-day expiry range; the one-hour default is uncalibrated. These guards are not a source/project duration, raster, codec or customer-tier limit. The in-memory proof covers exact streamed hash/length/metadata reread, idempotent collision, signed origin/key and rejection; it does not prove live bucket isolation, object lifecycle, multipart/resume, cancellation, throughput, latency or cost. Calibrate a large-object strategy and render lease SLO using real private R2 telemetry before promotion. Sources: [R2 limits](https://developers.cloudflare.com/r2/platform/limits/), [presigned URLs](https://developers.cloudflare.com/r2/api/s3/presigned-urls/), [S3 compatibility](https://developers.cloudflare.com/r2/api/s3/api/). |
| MEDIA-24 | Exact-render source-preparation jobs use a five-attempt ceiling and seven-day durable-job retention. | REMEDIATED_POLICY_BINDING_CALIBRATION_OPEN | The historical values in commit `828785eff` were provisional, not render SLOs or content limits. Commit `3ff5f760f` adds the versioned no-default declaration, and `e304a4563` removes both active literals: job creation now derives attempts/retention only from a declaration whose owner/version/hash matches the bound runtime receipt; publication rechecks the same lifecycle. Commit `c4aea45f0` makes the worker consume that same declaration for retry/dead-letter decisions. Test fixtures still choose five attempts/seven days for their own job cases, but no runtime owner defaults to them. Dispatcher migration and calibration from measured encoder duration, transient redelivery, storage recovery, resource pressure and cost remain mandatory before live promotion. |
| MEDIA-25 | A non-null opaque budget reservation on an exact-render source-preparation job means render-preparation cost accounting is complete. | PARTIAL_CONVERGENCE_LIVE_PROOF_TELEMETRY_OPEN | False. Commit `f7abef209` binds `{ reservationId, bindingSha256 }` into durable identity; `f1391d8e1` requires a matching authorization receipt and idempotent terminal reconciliation. Commits `c76aaacd1`, `01b3e7997`, `031ae00e5` and `912e4fd0b` culminate in mandatory V1.3 budget-owner ID/version/policy-digest binding and worker equality checks before authorization or settlement. Commit `37342c0ac` adds a no-default Finance policy and exact rational calculator; `3d5f605a0` adds exact scope/policy/maximum-cost authorization plus a content-bound internal-cost reservation reference; `3fb76b822` adds exact/released/conservative terminal reconciliation. Commit `b5790e0a5` adds the storage-agnostic transactional owner. Commit `0c20eae3c` adds the exact-policy Mongo locator and snapshot/majority/primary Mongo ledger adapter with deterministic insert-or-compare, persisted-envelope verification and reserved-record hash CAS. Commit `c14ce90f5` prevents successful retries from being mislabeled as exact. Commit `dcf553d26` adds the real worker adapter: it independently checks job/reservation/policy/scope/revision/request/expiry, issues a content-bound authorization receipt and derives exact-or-conservative terminal settlement from validated artifact/job evidence. Commit `b81a399ef` exposes that exact reservation/policy/scope/lifetime qualification for product use before a claim and makes post-claim authorization reuse the same Finance logic. Commit `498bdb8ee` invokes that pre-claim authority and composes the same owner into the exact-render product runtime before any durable attempt is claimed. The proof still uses an injected ledger: it moves no wallet and includes no configured-Atlas transaction, Finance-approved policy row, partial-attempt telemetry, calibrated rates or live redelivery. Those owners and evidence remain mandatory before production dispatch. |
| MEDIA-26 | Supplying syntactically valid budget-owner and retry-policy owner/version strings to the preparation worker binds those runtime policies to the durable job. | PARTIAL_CONVERGENCE_LIVE_POLICY_PROOF_OPEN | False. Commit `912e4fd0b` removes the V1.2 omission path: V1.3 requires the canonical receipt in payload, dependencies and operation identity, then compares injected budget/retry owner ID, version and policy SHA-256 before any corresponding owner call. Ten owner-binding drift variants fail before resource access. Commit `dcf553d26` adds the concrete budget-side worker owner and independently rechecks the reservation's scope, revision, request, policy and lifetime before authorization or settlement. Commits `498bdb8ee` and `0ac2507d9` compose that owner behind the signed application route. Syntactically valid strings alone remain insufficient: no configured historical Finance-policy row or Atlas ledger, calibrated retry timing/attempt/retention rule, or live render/recovery telemetry has been proven. |
| MEDIA-27 | An abort-aware source downloader, FFmpeg encoder, private R2 stager and durable preparation adapter mean exact-render durable cancellation is complete. | PARTIAL_CONVERGENCE | False. Commits `3b1553df9` and `3634f2133` propagate a caller signal through verified download, FFmpeg/FFprobe, local frame/PCM/hash work, conditional PUT and exact stored-object reread. Commit `4a4e87e1f` adds an adapter-owned abort controller and sequential heartbeat pump that rejects a late result after lease loss. The adapter is not instantiated by a production composition root; source-lease and every PCM/storage operation are not all proven cooperatively abortable; and no measured live long-running cancellation, recovery or partial-object cleanup proof exists. Do not treat this as complete cancellation or resource-release certification. |
| MEDIA-28 | Separating URL-free artifact preparation from fresh source-lease publication and adding its durable adapter means the exact-render path is connected. | PARTIAL_CONVERGENCE_LIVE_PROOF_AND_CONSUMPTION_OPEN | False. Commit `b94656858` gives the sole materializer owner a shared-core artifact-preparer entry point; commit `4a4e87e1f` adapts it to the durable worker contract with job revalidation and result validation. Commit `6141909c6` adds an authorized completed-result publisher; commits `31fedabbc` and `6ec1a293a` add the distinct source-rights authority/ledger. Commit `38fc8eaed` adds fixed-message initial/recovery dispatch through the shared state-bound QStash owner and exact historical-policy registry. Commit `36934bc98` adds request-time signed ingress for that strict message. Commit `498bdb8ee` composes the immutable manifest, deployed-image/toolchain checks, private-storage runtime, Finance pre-claim owner, current ProjectService revision, asset reader, sole materializer, encoder, heartbeat adapter and durable lifecycle worker. Commit `0ac2507d9` exports that runtime through the exact signed dispatcher path. No production principal adapter, configured live infrastructure, completed-source publication orchestration or Remotion consumption is wired yet. This is route-to-runtime convergence for preparation, not complete source-publication/render consumption or production proof. |
| MEDIA-29 | A heartbeat interval no greater than one-third of the current 300,000-ms durable lease is calibrated production policy. | PROVISIONAL_INFRASTRUCTURE_POLICY | False. Commit `4a4e87e1f` closes and content-hashes this conservative safety envelope so overlapping heartbeats and intervals near lease expiry fail before work. Commits `c76aaacd1` and `912e4fd0b` give it one canonical owner/version/digest slot, make that receipt mandatory in V1.3 and require the adapter-exposed heartbeat identity/digest to match before work or settlement. Neither interval nor the shared five-minute lease was calibrated against real multi-hour FFmpeg/R2 duration, scheduler jitter, Atlas latency, worker suspension, cancellation latency or cost. Measure those conditions and prove live redelivery before promotion. |
| MEDIA-30 | The source-media consent collected at upload and stored as native-video `audioRights` is a complete current visual-and-audio source clearance authority for exact final rendering. | MISSING_WIRING | False. Upload UI language covers the media including embedded audio, but the server currently materializes that consent through `buildNativeVideoAudioRights` and stores it under `audioRights`; the existing render authority verifies audio claims. Commit `31fedabbc` adds a distinct project/use/source-version-bound general rights record, principal-authorized revocation and the publication adapter that invokes the existing audio authority for exact-PCM artifacts. Commit `6ec1a293a` persists that separate authority through an exact project/source-version ledger and intentionally never promotes legacy `audioRights`. A production principal/membership adapter, versioned legal terms/disposition UI, explicit re-attestation/migration policy, composition root and live Atlas proof remain mandatory. |
| MEDIA-31 | Project-scoping every source-media rights record proves the final legal/product licence model for every customer and territory. | CONSERVATIVE_PROVISIONAL_POLICY | False. Commit `31fedabbc` deliberately requires an exact project for user-owned, org-owned and licensed dispositions so a broad upload checkbox cannot silently authorize unrelated exports. This is a safety boundary, not legal advice or proof of the final cross-project library policy. A qualified legal/product owner must version and hash the actual terms, define any reusable-library grant explicitly, and preserve per-source-version revocation and auditability; until then, new project use requires explicit re-attestation. |
| MEDIA-32 | A principal-authority receipt hash inside a valid rights record means membership/asset access and rights persistence are live. | MISSING_WIRING | False. The trusted owner port in commit `31fedabbc` is invoked during issuance and its owner/version/receipt hash is bound into the record. Commit `6ec1a293a` adds a dedicated per-tenant/project/asset/source-version CAS head plus immutable transition-event ledger, but no Clerk/org/project membership adapter or authenticated product composition invokes it and its Mongo transaction has no live Atlas proof. Caller-authored receipt hashes are never authoritative. Production issuance must obtain the receipt from the real access owner, persist through this sole ledger, and reject concurrent source or authorization change. |
| MEDIA-33 | The project-scoped source-rights ledger means rights persistence and legal eligibility are production-complete. | MISSING_WIRING | False. Commit `6ec1a293a` proves pure transition/CAS behavior, project isolation, chain integrity and a typed transactional Mongo adapter. The final-render adapter consumes the injected ledger reader, not `MEDIA_ASSETS` fields. No authenticated issuance/re-attestation/revocation surface, production principal owner, legacy-asset migration, legal/product terms approval, composition root, live Atlas transaction/redelivery exercise, operational audit query or backup/recovery proof exists. Missing ledger evidence remains a render blocker. |
| MEDIA-34 | A canonical exact-render runtime-policy receipt or execution manifest means those policies are already enforceable by durable production execution. | PARTIAL_CONVERGENCE_LIVE_MANIFEST_PROOF_OPEN | False as a production claim. Commit `912e4fd0b` closes the local contract/worker gap: V1.3 makes the receipt mandatory, terminal proof remains bound through the job-input hash, and budget/retry/heartbeat owner ID, version and digest drift rejects before resource access. Commit `90230a4cc` adds the full historical execution manifest: the existing domain owners validate materializer/encoder/private-artifact limits; the manifest binds those exact declarations plus retry, heartbeat, execution-budget reference, worker image and compatibility receipt, then requires exact job equality. Commit `6d8dc6b9c` adds create-only Mongo registration and exact six-part lookup with unique indexes, majority write, primary reread and idempotent identical replay. Commit `e70f6f310` adds an operation-owner/kind/input-schema-scoped read that resolves the durable job without claiming or spending an attempt and rejects malformed scope before opening Mongo. Commit `8e03cb0d8` makes the job contract the single production source for that operation owner/kind across creation, worker, budget, materializer adapter and publication. Commit `353dd72f3` exposes the encoder owner's exact FFmpeg/FFprobe/OS/architecture/Remotion/compositor qualification for reuse before a durable claim, while retaining the same check inside encode. Commit `e1e70323a` makes that job contract the sole full durable-envelope validator and makes the lifecycle worker consume it, so the product runner need not copy binding checks. Commit `498bdb8ee` makes the product runtime resolve that manifest, bind the deployed image and invoke the exact toolchain qualifier before Finance, ProjectService or durable claim, while preserving historical terminal settlement. Commit `0ac2507d9` exports the signed application route for that runtime. Proof remains injected: there is no configured-Atlas row/index, approved active declaration, real deployment rotation, calibrated policy, hosted redelivery or deployment evidence. Therefore durable production enforcement and dispatch remain unproven. |
| MEDIA-35 | A content-hashed exact-render delivery/retry declaration is calibrated production policy or already controls all job and transport behavior. | PARTIAL_CONVERGENCE | False. Commit `3ff5f760f` requires explicit attempt, retention, QStash delivery and worker-delay values and issues hash-bound retry/dead-letter decisions. Commit `e304a4563` makes job creation and completed-result publication enforce the exact declaration and lifecycle; commit `c4aea45f0` makes the worker consume it. Commit `38fc8eaed` makes initial/recovery dispatch consume the active or exact retained declaration's QStash values and blocks an unknown historical policy without publishing. Commit `36934bc98` authenticates a strict worker message at request time and maps retry/lease outcomes without inventing delay; commits `498bdb8ee` and `0ac2507d9` compose and export that lifecycle. Terminal decision evidence is not yet durable and no selected values are calibrated. Measure real encoder/storage/Atlas/QStash behavior, version the selected declaration and prove hosted redelivery/cost before calling it calibrated or fully enforced. |
| MEDIA-36 | Supplying the current deployment's exact-render retry policy is enough to recover every retained durable job after policy rotation. | PARTIAL_CONVERGENCE_OPERATIONAL_RETENTION_OPEN | False. V1.3 binds owner ID, owner version and policy SHA-256; commit `2117efc83` adds the exact immutable active/retained resolver. Commit `38fc8eaed` wires recovery through that resolver and proves an old job publishes with its retained old QStash values after active rotation, while an unknown old policy publishes nothing. No calibrated production registry is composed or deployed, and code cannot prove operators retain every entry for at least the longest governed job lifetime. Retain old declarations operationally and prove old-job recovery after a real deployment rotation before closing this debt. |
| MEDIA-37 | A safely dead-lettered exact-render preparation job durably proves which retry-policy decision caused terminalization. | MISSING_TERMINAL_EVIDENCE | False. Commit `c4aea45f0` computes a content-hashed `ATTEMPTS_EXHAUSTED` or `RETENTION_EXHAUSTED` decision and passes a non-retryable failure to the durable store, so unsafe/expired retry does not occur. The shared V1.1 store deliberately clears `retryCursor` on terminal transition, however, so the decision hash and reason are not retained after dead-letter. Before production, add a versioned terminal-failure evidence contract and persist it atomically with dead-letter state; prove redelivery/idempotency and retain the original failure plus policy binding. Logs or an in-memory decision are not substitutes. |
| MEDIA-38 | The Finance meter's 40-digit decimal-string ceiling limits supported project duration, frame count, artifact size or price. | PARSER_RESOURCE_CEILING | False. Commit `37342c0ac` bounds every untrusted decimal before `BigInt` multiplication and also bounds the calculated nano-USD total. This is a hostile-input/resource ceiling many orders above the currently qualified artifact limits, not a media or commercial capacity claim. Revisit only if measured supported workloads or approved Finance rationals require a larger representation; never silently truncate, float-convert or clamp. |
| MEDIA-39 | The exact-render worker route's 800-second `maxDuration` proves support for any long-form render that fits beneath it, or is a calibrated render SLO. | PROVISIONAL_INFRASTRUCTURE_POLICY | False. Commit `0ac2507d9` uses the repository's existing long-worker deployment class so the new signed route does not inherit a much shorter platform default. The static ceiling is neither a source/project duration limit nor proof that the hosting plan, QStash timeout, durable lease, heartbeat, FFmpeg process, R2 staging and termination cleanup align. Calibrate them together with real short/long renders and forced termination/redelivery; move long work to a qualified durable compute owner if the measured render envelope cannot finish safely inside the route ceiling. |
| MEDIA-40 | Reconstructing one V3 epoch from a terminal V2 timestamp map means legacy migration can discover timestamp resets or that V2-to-V3 persistence is complete. | LOCAL_MIGRATION_COMPLETE_LIVE_AND_DIRECT_SCAN_OPEN | False as a broad claim. Commit `ad57ac39f` rereads and reverifies every immutable V2 artifact and prepares one truthful `INITIAL` epoch only for a globally contiguous sequence. Commit `90de32b46` adds create-only epoch-index persistence with exact reread. Commit `77b78c90e` then performs one exact V2-state-to-V3-state `MEDIA_ASSETS` CAS and persists the migration receipt atomically. V2 still has no discontinuity-boundary evidence to migrate, and no live bucket/Atlas document or production selection caller was exercised. Direct V3 scanning/finalization, dedicated private PTS configuration and live proof remain mandatory; never infer extra epochs or generalize contiguous migration into discontinuity support. |
| MEDIA-41 | A create-only V3 epoch-index writer with exact in-memory-client reread proves dedicated private PTS storage is configured and operational. | MISSING_LIVE_INFRASTRUCTURE | False. Commit `90de32b46` composes the writer into the sole private media-evidence R2 runtime, rejects public scope, uses `If-None-Match: *`, and accepts replay only after exact content reread. No `EDITRON_MEDIA_PTS_R2_*` credential set or live private object was available on the audited machine. Provision the dedicated non-browser bucket and least-privilege credentials, then prove create, identical replay, altered-collision rejection, full reread, access denial from browser/public paths, lifecycle/retention policy and recovery before promotion. Generic R2 credentials or the public CDN bucket are forbidden substitutes. |
| MEDIA-42 | Code-level `updateOne` CAS and an atomically stored migration receipt prove live Atlas migration, durable audit consumption and operational recovery. | MISSING_LIVE_PROOF_AND_RECEIPT_CONSUMER | False. Commit `77b78c90e` reuses the exact V2 filter, adds absent-V3/receipt guards, and installs V3 plus the receipt in one document update. The tests use injected ports and do not exercise a configured Atlas document, write concern, retry/redelivery, backup/restore or a normal reader that validates and reports the persisted receipt. Add the receipt read/audit owner, a bounded authenticated selection/dispatch path, and a disposable live fixture proving success, stale race, idempotent follow-up, exact cleanup and recovery before promotion. |
| MEDIA-43 | A safe compute-only V3 splitter means reset, wrap and edit-list support—or the production V3 scanner—is complete. | PARTIAL_CONVERGENCE_PUBLICATION_AND_EXTERNAL_EVIDENCE_OPEN | False. Commit `f16003eb5` preserves exact timestamps, keeps ordinary VFR contiguous and splits only gaps or positive-start overlaps proven by timestamp arithmetic. It deliberately blocks repeated/backward timestamps because generic FFprobe frame output cannot distinguish a reset, counter wrap, edit-list remap or corrupt ordering. Commits `f4fc3f944`, `35390cc40`, `34f4fcea9` and `1754b28e4` select the primitive through a distinct Modal function, endpoints, TypeScript HTTP transport and contextual segmented-result admission. Commit `c4fd71adc` adds a direct-V3 preparation consumer that rereads every promoted frame and classifies only arithmetic-proven contiguous/GAP/OVERLAP handoffs; it still does not publish state. Add container-specific MPEG-TS discontinuity, MOV edit-list and counter-wrap evidence owners; bind them to source/version/range and immutable artifacts; then wire durable publication and live proof. Never infer a semantic boundary label from a negative delta alone. |
| MEDIA-44 | A separately named V3 Modal function means product callers can submit, poll, finalize or publish direct V3 scans. | PARTIAL_CONVERGENCE_DURABLE_JOB_PUBLICATION_AND_DEPLOYMENT_OPEN | False. Commits `f4fc3f944` and `35390cc40` provide exact V3 function/endpoint identities. Commit `34f4fcea9` adds an identity-bound TypeScript client while preserving the separate V1 domain contract over shared HTTP mechanics, and commit `c4fd71adc` prepares a terminal V3 result into exact epoch/index bytes. No product composition or durable lifecycle selects that path, no owner writes and CAS-publishes the prepared result, and no deployment/live call exists. Add those owners and preserve the V1 lane unchanged; a callable transport plus in-memory preparation is not a completed product scan path. |
| MEDIA-45 | Passing local endpoint tests means the V3 Modal app, proxy authentication and dedicated private PTS R2 storage are deployed and reachable. | MISSING_LIVE_DEPLOYMENT_AND_PRIVATE_STORAGE_PROOF | False. The 49 Modal tests run without network and prove local contract selection, validation and response binding. No Modal deployment, endpoint URL, proxy-auth exchange, allowed source fetch, dedicated private-bucket write/reread, failure redelivery or access-denial check occurred. Provision the exact private PTS configuration, deploy a manifest-qualified app and exercise a rights-cleared disposable source through authenticated submit/poll plus exact cleanup before promotion. |
| MEDIA-46 | Because the V3 mapper and transport reuse V1 scan-request, staging-batch and terminal-result schemas, a canonical V3 epoch index already exists or the V1 and V3 pipelines are merged. | PARTIAL_CONVERGENCE_SHARED_STAGING_PUBLICATION_OPEN | False. Commit `34f4fcea9` deliberately reuses the exact immutable per-run evidence format; mapper/policy identity and a distinct submission/job contract select V3 behavior. Commit `c4fd71adc` now rereads/promotes those staged inputs and prepares a canonical V3 index, but does not persist or publish it. The publication owner must write/reverify the immutable index and CAS-publish it through the existing V3 asset lifecycle. V1 retains a separate finalizer and publication path. This is shared downstream evidence plumbing, not merged control flow. |
| MEDIA-47 | Strictly increasing batch starts in a V3 terminal summary prove the exact frame-level GAP/OVERLAP boundary and are sufficient for publication. | EXACT_ARTIFACT_REPROOF_VERIFIED_PUBLICATION_OPEN | False as a summary claim. Commit `1754b28e4` repairs the prior impossible V1-continuity admission and preserves the V1 raw-result schema under exact V3 mapper/policy context. A summary exposes the prior batch start/end but not its immediately preceding frame PTS, so it cannot by itself prove the Modal producer rule `current PTS > previous frame PTS`. Commit `c4fd71adc` supplies the required direct-V3 artifact reproof: it rereads every immutable staging object, validates each run, compares actual previous-last-frame PTS/duration to the next first-frame PTS, classifies only exact GAP/positive-start OVERLAP, and blocks the deceptive `0,100 -> 50` case. Index storage and `MEDIA_ASSETS` CAS remain separate open work; a distinct V3 wire schema may be introduced later, but must not substitute for artifact re-verification. |
| MEDIA-48 | An in-memory `PREPARED` direct-V3 finalization result means the epoch map is durably published and recoverable. | MISSING_ATOMIC_PUBLICATION_AND_LIFECYCLE_COMPOSITION | False. Commit `c4fd71adc` returns exact serialization and sidecar material only after immutable batch promotion and frame-level reproof. It intentionally performs no epoch-index write and no `MEDIA_ASSETS` mutation. Commit `542a7d928` supplies bounded same-claim renewal in the sole V3 state owner, but no finalizer invokes it yet. Compose create-only private storage with expected-source `PENDING` CAS, owned `VERIFYING` claim/renewal, exact post-write verification and terminal `COMPLETE`/failure CAS; prove crash/redelivery, stale claim, storage collision/outage, retryable verifier reads and idempotent replay. Never call the V1 finalizer or treat returned bytes as state publication. |
| MEDIA-49 | A tested same-claim renewal function means a production V3 verifier cannot lose ownership during long artifact reads. | MISSING_CALLER_AND_LIVE_LONG_RUNNING_PROOF | False. Commit `542a7d928` preserves claim ID/attempt count, advances a bounded lease window and admits that transition through state-hash/claim/expiry CAS. It rejects foreign, expired, shortening, oversized and forged renewals. No durable finalizer schedules renewal before expiry, heartbeats its job lease around the CAS, or proves a multi-renewal run, process pause, CAS race and post-expiry takeover against live Atlas. The publication caller must renew at measured safe checkpoints and stop immediately on any lost CAS; the pure owner is necessary but not operational proof. |

## J. Product execution-root boundaries

| ID | Hardcode or assumption | Flag | Current evidence and required disposition |
| --- | --- | --- | --- |
| ROOT-01 | A product execution composition root means the 40-operation research catalog is product-executable. | PARTIAL_CONVERGENCE | False. Commit `f5b192f2c` deliberately admits only the concrete ProjectService-clone writers `cut_section` and `set_keyframes`. `apply_audio_ducking` is catalog-permitted research work but is rejected before budget, media, project or provider resource access because this root has no concrete isolated writer for it. Every additional operation needs its existing sole owner, exact state effects, deterministic replay and proof wired into this same dispatcher; never add a fallback writer. |
| ROOT-02 | The composition root and exported signed route are a live product workflow or authorize canonical project mutation. | PARTIAL_LIVE_PROOF | False as a broad claim. Commit `a481f4d32` proves one development cancellation-recovery path through real Atlas job/wallet transactions, the official local QStash emulator and the signed route: one post-settlement crash redelivers to `DELIVERED`, releases the reservation exactly once, keeps the job cancelled with zero attempts and creates no project row. It does not prove hosted QStash/deployment, provider attempts, `ACTUAL_USAGE`, Finance-policy lookup, customer charging or canonical ProjectService apply. Canonical mutation remains disabled until separately authorized and proven. |
| ROOT-03 | The Finance pricing implementation chooses or proves Editron's customer rate. | MISSING_LIVE_POLICY | False. Commit `2e030edfd` implements exact policy lookup and integer charge derivation but deliberately seeds no rate. A Finance-approved owner/version/hash row must exist and cover the approval window before execution; missing or drifted policy fails closed. The 27/27 dependent tests use injected records and do not prove live Mongo lookup, wallet movement, gross margin or production unit economics. |
| ROOT-04 | Requiring both provider credential families and `EDITRON_PROVIDER_NATIVE_STORAGE_READ_TIMEOUT_MS` proves the final deployment policy. | PROVISIONAL_DEPLOYMENT_POLICY | Commit `c5c49a593` deliberately blocks the shared route unless its three advertised model routes can resolve both credential families and unless storage reads have an explicit 1-300 second bound. This prevents post-claim configuration failure; it is not evidence that one shared multi-provider route or those limits are the final production topology. Revisit only with route-specific dispatch eligibility and measured storage latency/cancellation evidence. |
| ROOT-05 | The checked-in development `QSTASH_URL` identifies the local QStash emulator. | INVALID_ENVIRONMENT_ASSUMPTION | False on the audited machine: `127.0.0.1:8080` is EnterpriseDB Apache and returns an HTML landing page/404 for QStash publish. Commit `a481f4d32` was proven by starting the bundled official QStash emulator on an isolated port and supplying process-only URL/token/signing-key overrides to both publisher and signed worker. Repair and verify the normal development environment explicitly; never silently fall through to hosted/production QStash. |

## K. Durable dispatch assumptions

| ID | Hardcode or assumption | Flag | Current evidence and required disposition |
| --- | --- | --- | --- |
| QSTASH-01 | The cadence/editorial durable-workflow dispatchers' former `headers: { 'Upstash-Timeout': '300s' }` configured the provider delivery timeout. | REMEDIATED_NARROWLY | The installed `@upstash/qstash` SDK exposes first-class `timeout`, `retries`, `retryDelay` and `deduplicationId` publish fields; arbitrary `headers` are forwarded to the destination. Commits `525eda578` and `9a2ed81cc` migrate these two callers to the shared owner, remove the header and make delivery policy mandatory input. Their producer, durable-store and fixed signed-worker control flow is verified. This does not certify the policy values, live redelivery or unrelated QStash families. |
| QSTASH-02 | A documented provider deduplication-window duration can be used as Editron's exactly-once or recovery guarantee. | EXTERNAL_POLICY_CONFLICT | Current provider web guidance and the installed SDK comments state different deduplication durations. Commit `7fccc4c22` therefore supplies a stable deduplication identity but makes no duration claim. Correctness must come from the durable job record, exact operation/idempotency identity, durable dispatch-receipt write and state-revalidating worker; provider deduplication is only an additional transport aid. Reconcile the deployed provider/version documentation operationally, but never weaken those local authorities. |
| QSTASH-03 | A recovery-state hash makes durable-store selection, QStash publication and dispatch-receipt persistence one atomic compare-and-set. | MISSING_ATOMIC_BOUNDARY | False. Commit `1277fda61` rederives a canonical binding over the exact active snapshot selected for recovery and uses it as the transport deduplication identity. The provider publish still precedes `recordDispatch`, whose late-acknowledgement semantics intentionally do not compare the selected snapshot. Concurrent state change can therefore create a harmless stale delivery or `delivery_unknown`; the current store/worker must recheck lifecycle authority and skip it. Do not claim exactly-once or scan-to-publish CAS without a separately designed outbox/transactional-dispatch boundary and live redelivery proof. |
| QSTASH-04 | Migrating the cadence and editorial durable-workflow callers means every Editron QStash publisher now uses one transport owner and correct first-class fields. | PARTIAL_CONVERGENCE | False. The 2026-08-30 same-HEAD source inventory finds 31 files with direct-client or legacy-header patterns and 13 files still containing `Upstash-Timeout`, spanning analysis workers, chat jobs, media intake, Auto Edit, render evidence and finalization. Some have different lifecycle/authentication requirements and cannot be bulk-replaced. Queue item 7 must classify each producer, decision owner, state source, consumer, retry/timeout policy and recovery semantics, then migrate in bounded families with signed ingress and redelivery proof. Until then, convergence applies only to the two durable-workflow callers above. |
