# Codebase Graph (lib/editron/)

## Source
Generated by graphify on 2026-05-23 from `editron-worktree/lib/editron/`
- Full report: `D:\google downloads\Front-End-main\graphify-out\GRAPH_REPORT.md`
- Interactive HTML: `D:\google downloads\Front-End-main\graphify-out\graph.html`
- Raw JSON: `D:\google downloads\Front-End-main\graphify-out\graph.json`

## Scale
- **127 files** · ~384,338 words
- **1673 nodes** · **2904 edges** · **96 communities**
- Extraction: 98% extracted, 2% inferred

## God Nodes (most connected — your core abstractions)

| # | Node | Edges | Community | Blast Radius |
|---|------|-------|-----------|-------------|
| 1 | `aliasMap` | 92 | Creative Knowledge Graph | Touches every CRG signal/mapping |
| 2 | `getDatabase()` | 74 | Project Database | Cross-community bridge (14 communities) |
| 3 | `executeDirectorPlan()` | 66 | Director Pipeline | **CRITICAL: connects 25+ communities. Betweenness 0.245.** |
| 4 | `runQualityReview()` | 53 | Quality Review | Wide check surface |
| 5 | `buildSignalTimeline()` | 30 | Signal Registry | Core signal computation |
| 6 | `ProjectService` | 26 | Project Database | Data access layer |
| 7 | `AssetResolver` | 18 | Segment Analysis | Asset lookup |
| 8 | `runFullAnalysis()` | 16 | 5-Track Analysis | Per-asset analysis |

## Key Communities for Visual Intelligence Work

| Community | Name | Nodes | Why It Matters |
|-----------|------|-------|----------------|
| 0 | Creative Knowledge Graph | 92 | All signal definitions live here |
| 10 | Signal Registry | 29 | `buildSignalTimeline()` — L0 signals would be added here |
| 11 | Signal Executor | 29 | Maps signals → decisions. Routing logic goes here. |
| 15 | Mode 2 Pipeline | 16 | Non-speech editing path (silence removal, best takes) |
| 16 | Director Pipeline | 21 | `executeDirectorPlan()` — THE god function |
| 25 | EDL/Decisions | 19 | `deduplicateDecisions()`, edit decisions |
| 30 | Brief Overrides | 14 | `applyBriefOverrides()`, transition dedup |
| 33 | Beat Analysis | 15 | `analyzeBeatsFull()`, BPM, spectral flux |
| 42 | Audio Analysis | 13 | `analyzeClipAudioService()`, filler/silence detection |
| 48 | Transcript Editor | 12 | `editTranscript()`, prompt building |

## Surprising Connections (INFERRED)
- `executeDirectorPlan()` → `toVjepaWeightFormat()` (V-JEPA integration)
- `executeDirectorPlan()` → `integrateVjepaScores()` (moment weights)
- `executeDirectorPlan()` → `toWav2VecWeightFormat()` (Wav2Vec integration)
- `executeDirectorPlan()` → `addGraphitiEpisode()` (graph service)

## Knowledge Gaps
- **582 isolated nodes** (≤1 connection) — possible missing edges or undocumented components

## Implications for Visual Intelligence
1. **`executeDirectorPlan()`** is the insertion point for routing logic. It already calls V-JEPA and Wav2Vec services. Adding L0 signal routing here is architecturally consistent.
2. **Community 10 (Signal Registry)** is where L0 signals (silence, beats, scene boundaries) would be added. `buildSignalTimeline()` is the entry point.
3. **Community 42 (Audio Analysis)** already has `detectSilenceGaps()` and `detectFillerWords()`. L0 silence detection should extend this, not duplicate.
4. **Community 33 (Beat Analysis)** already has `analyzeBeatsFull()`, BPM estimation, spectral flux. Essentia.js integration would extend or replace this.
5. **Community 15 (Mode 2)** has the non-speech editing primitives (`buildSilenceRemovalPlan()`, `detectBestTakes()`). TAG routing could leverage these.

Tags: #architecture #graph #codebase
