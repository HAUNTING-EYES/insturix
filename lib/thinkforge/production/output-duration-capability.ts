/**
 * Maximum duration for a ThinkForge video deliverable that can be persisted
 * and handed to Editron. Writer capacity is resolved separately per request.
 */
export const THINKFORGE_MAX_PRODUCTION_OUTPUT_DURATION_SECONDS = 24 * 60 * 60;
