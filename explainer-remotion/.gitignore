# Standalone Remotion render sub-project — spawned by the explainer worker, never imported by the app.
# Everything below is per-run state regenerated by scripts/craft-and-render.mjs (prep-audio + agent-craft).
node_modules/
out/
# craft-generated scenes/manifest/plan + prep-audio config (rewritten every run)
src/bricks/gen/
src/bricks/generated-plan.ts
# per-run VO + downloaded brand screenshots (real committed product stills like app-*.png stay tracked)
public/audio/
public/product/scan-*.png
