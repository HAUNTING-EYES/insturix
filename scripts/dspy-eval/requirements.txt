# DSPy Evaluation Framework for Insturix Scene Parser
# Install: pip install -r requirements.txt

dspy-ai>=2.5
google-generativeai>=0.8.0
python-dotenv>=1.0.0

# Optional: for better JSON diffing in ground truth builder
deepdiff>=7.0
